# frozen_string_literal: true

source "https://rubygems.org"
gemspec

# TODO: This is a temporary change to ensure that CI tests for #114 pass.
#       Revert changes to this file before merging.
gem "jekyll", :github => "jekyll/jekyll", :ref => "refs/pull/7554/head"
# gem "jekyll", ENV["JEKYLL_VERSION"] ? "~> #{ENV["JEKYLL_VERSION"]}" : ">= 4.0"
gem "minima"
